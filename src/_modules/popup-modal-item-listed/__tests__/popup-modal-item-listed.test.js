'use strict';

import PopupModalItemListed from '../popup-modal-item-listed';

describe('PopupModalItemListed View', function() {

  beforeEach(() => {
    this.popupModalItemListed = new PopupModalItemListed();
  });

  it('Should run a few assertions', () => {
    expect(this.popupModalItemListed);
  });

});
