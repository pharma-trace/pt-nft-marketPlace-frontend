'use strict';

import PopupModalReviewInfoBuyBid from '../popup-modal-review-info-buy-bid';

describe('PopupModalReviewInfoBuyBid View', function() {

  beforeEach(() => {
    this.popupModalReviewInfoBuyBid = new PopupModalReviewInfoBuyBid();
  });

  it('Should run a few assertions', () => {
    expect(this.popupModalReviewInfoBuyBid);
  });

});
