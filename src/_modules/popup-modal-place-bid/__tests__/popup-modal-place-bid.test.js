'use strict';

import PopupModalPlaceBid from '../popup-modal-place-bid';

describe('PopupModalPlaceBid View', function() {

  beforeEach(() => {
    this.popupModalPlaceBid = new PopupModalPlaceBid();
  });

  it('Should run a few assertions', () => {
    expect(this.popupModalPlaceBid);
  });

});
