'use strict';

import PopoverFees from '../popover-fees';

describe('PopoverFees View', function() {

  beforeEach(() => {
    this.popoverFees = new PopoverFees();
  });

  it('Should run a few assertions', () => {
    expect(this.popoverFees);
  });

});
