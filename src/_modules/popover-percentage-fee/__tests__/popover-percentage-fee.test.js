'use strict';

import PopoverPercentageFee from '../popover-percentage-fee';

describe('PopoverPercentageFee View', function() {

  beforeEach(() => {
    this.popoverPercentageFee = new PopoverPercentageFee();
  });

  it('Should run a few assertions', () => {
    expect(this.popoverPercentageFee);
  });

});
