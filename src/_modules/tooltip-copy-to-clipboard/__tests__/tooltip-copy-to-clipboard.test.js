'use strict';

import TooltipCopyToClipboard from '../tooltip-copy-to-clipboard';

describe('TooltipCopyToClipboard View', function() {

  beforeEach(() => {
    this.tooltipCopyToClipboard = new TooltipCopyToClipboard();
  });

  it('Should run a few assertions', () => {
    expect(this.tooltipCopyToClipboard);
  });

});
