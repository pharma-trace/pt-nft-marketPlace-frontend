'use strict';

import PopoverEmailAddress from '../popover-email-address';

describe('PopoverEmailAddress View', function() {

  beforeEach(() => {
    this.popoverEmailAddress = new PopoverEmailAddress();
  });

  it('Should run a few assertions', () => {
    expect(this.popoverEmailAddress);
  });

});
