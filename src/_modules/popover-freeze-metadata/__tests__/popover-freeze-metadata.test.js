'use strict';

import PopoverFreezeMetadata from '../popover-freeze-metadata';

describe('PopoverFreezeMetadata View', function() {

  beforeEach(() => {
    this.popoverFreezeMetadata = new PopoverFreezeMetadata();
  });

  it('Should run a few assertions', () => {
    expect(this.popoverFreezeMetadata);
  });

});
