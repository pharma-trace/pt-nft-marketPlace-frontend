'use strict';

import ModalAdvanceSearch from '../modal-advance-search';

describe('ModalAdvanceSearch View', function() {

  beforeEach(() => {
    this.modalAdvanceSearch = new ModalAdvanceSearch();
  });

  it('Should run a few assertions', () => {
    expect(this.modalAdvanceSearch);
  });

});
