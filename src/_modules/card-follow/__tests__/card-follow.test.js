'use strict';

import CardFollow from '../card-follow';

describe('CardFollow View', function() {

  beforeEach(() => {
    this.cardFollow = new CardFollow();
  });

  it('Should run a few assertions', () => {
    expect(this.cardFollow);
  });

});
