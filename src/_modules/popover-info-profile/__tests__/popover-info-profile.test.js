'use strict';

import PopoverInfoProfile from '../popover-info-profile';

describe('PopoverInfoProfile View', function() {

  beforeEach(() => {
    this.popoverInfoProfile = new PopoverInfoProfile();
  });

  it('Should run a few assertions', () => {
    expect(this.popoverInfoProfile);
  });

});
