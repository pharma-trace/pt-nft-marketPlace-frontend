'use strict';

import ModalMenu from '../modal-menu';

describe('ModalMenu View', function() {

  beforeEach(() => {
    this.modalMenu = new ModalMenu();
  });

  it('Should run a few assertions', () => {
    expect(this.modalMenu);
  });

});
