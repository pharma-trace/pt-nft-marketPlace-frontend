'use strict';

import PopoverAccountProfile from '../popover-account-profile';

describe('PopoverAccountProfile View', function() {

  beforeEach(() => {
    this.popoverAccountProfile = new PopoverAccountProfile();
  });

  it('Should run a few assertions', () => {
    expect(this.popoverAccountProfile);
  });

});
