'use strict';

import PopoverLastUpdate from '../popover-last-update';

describe('PopoverLastUpdate View', function() {

  beforeEach(() => {
    this.popoverLastUpdate = new PopoverLastUpdate();
  });

  it('Should run a few assertions', () => {
    expect(this.popoverLastUpdate);
  });

});
