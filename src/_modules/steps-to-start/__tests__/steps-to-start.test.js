'use strict';

import StepsToStart from '../steps-to-start';

describe('StepsToStart View', function() {

  beforeEach(() => {
    this.stepsToStart = new StepsToStart();
  });

  it('Should run a few assertions', () => {
    expect(this.stepsToStart);
  });

});
