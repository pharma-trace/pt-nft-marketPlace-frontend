'use strict';

import PopupModalRecaptcha from '../popup-modal-recaptcha';

describe('PopupModalRecaptcha View', function() {

  beforeEach(() => {
    this.popupModalRecaptcha = new PopupModalRecaptcha();
  });

  it('Should run a few assertions', () => {
    expect(this.popupModalRecaptcha);
  });

});
