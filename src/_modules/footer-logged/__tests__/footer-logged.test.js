'use strict';

import FooterLogged from '../footer-logged';

describe('FooterLogged View', function() {

  beforeEach(() => {
    this.footerLogged = new FooterLogged();
  });

  it('Should run a few assertions', () => {
    expect(this.footerLogged);
  });

});
